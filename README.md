 git init
 git remote add origin https://github.com/ChaitanyaKiran14/redis-pubsub.git
 git add .
 git commit -m "lgtm"
 git branch -a          to check the existing branches
 git checkout -b dev    to create and switch to that particular branch
 git push -u origin dev
